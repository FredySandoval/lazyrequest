{
  "name": "lazyrequest",
  "version": "1.0.2",
  "devDependencies": {
    "@fredy-dev/http-parser": "^2.0.0",
    "@types/bun": "latest",
    "@typescript/native-preview": "^7.0.0-dev.20260215.1",
    "commander": "^14.0.3",
    "fast-glob": "^3.3.3",
    "pino": "^10.3.1",
    "typescript": "^5"
  },
  "description": "A lightweight minimal API Testing Client tool inspired by REST Client syntax.",
  "scripts": {
    "build": "bun run script/build-binaries.ts",
    "package": "bun run script/package-binaries.ts",
    "publish:npm": "bun run script/publish-npm.ts",
    "test": "bun test --concurrent --timeout 30000",
    "typecheck": "tsgo --noEmit"
  }
}
